# Adaptive RRF Weight Matrix (v17)
# weights: [dense, sparse, graph, visual/community]
# v17: 6 query types with intent-specific channel weights

factoid:
  weights: [0.80, 0.20, 0.00, 0.00]
  description: "事实类查询，纯向量检索即可，跳过图谱"

entity_relation:
  weights: [0.30, 0.00, 0.70, 0.00]
  description: "实体关系查询，图谱为主要通道"

multi_hop:
  weights: [0.15, 0.00, 0.85, 0.00]
  description: "多跳推理，图谱优先"

global_summary:
  weights: [0.20, 0.00, 0.00, 0.80]
  description: "全局总结，社区摘要为主要通道"

temporal:
  weights: [0.35, 0.20, 0.45, 0.00]
  description: "时间敏感查询，Dense+Graph 均衡"

comparison:
  weights: [0.35, 0.25, 0.35, 0.05]
  description: "对比分析，多通道均衡"

# v12 backward-compatible entries
L1_FACTUAL:
  weights: [0.70, 0.25, 0.00, 0.05]
  description: "v12 L1 兼容"

L2_REASONING:
  weights: [0.20, 0.10, 0.65, 0.05]
  description: "v12 L2 兼容"

L3_MACRO_SUMMARY:
  weights: [0.35, 0.20, 0.35, 0.10]
  description: "v12 L3 兼容"

DEFAULT:
  weights: [0.40, 0.30, 0.15, 0.15]
  description: "默认权重"
