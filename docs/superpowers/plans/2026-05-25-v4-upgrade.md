# Ragent AI v4.0 — 知识库一致性治理 & 评估体系 升级计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现跨库级联软删除、实体自动消歧、时序图谱增强、自动化评估管道、RRF 权重网格搜索、CI/CD 流水线 6 项升级。

**Architecture:** 本计划分为 6 个 Phase，Phase 1-3 属于知识库治理（底层存储一致性与图谱质量），Phase 4-6 属于量化评估与工程化（实验数据与 CI/CD）。Phase 1-3 之间有一定依赖（实体消歧依赖软删除的 Neo4j 清理能力），Phase 4-5 有依赖（网格搜索依赖评估框架），Phase 6 完全独立。

**Tech Stack:** Python 3.12 · FastAPI · SQLAlchemy · Neo4j/APOC · Milvus · pytest · Ragas · GitHub Actions · Docker

---

## 文件结构概览

```
新增文件 (14):
  backend/storage/doc_lifecycle.py          # 文档生命周期状态机
  backend/storage/graph_cleanup.py           # Neo4j Cypher 清理脚本
  backend/graph/entity_resolution.py         # 两阶段实体消歧与融合
  backend/evaluation/__init__.py             # 评估模块入口
  backend/evaluation/metrics.py              # Ragas 指标计算
  backend/evaluation/dataset.py              # 黄金数据集加载
  scripts/run_evaluation.py                  # 评估入口脚本
  scripts/grid_search_rrf.py                 # RRF 权重网格搜索
  scripts/run_entity_resolution.py           # 实体消歧独立脚本
  tests/__init__.py
  tests/test_doc_lifecycle.py                # 软删除单元测试
  tests/test_entity_resolution.py            # 实体消歧单元测试
  tests/golden_dataset.json                  # 黄金评估数据集
  .github/workflows/ci.yml                   # GitHub Actions CI 流水线
  Dockerfile                                 # 应用容器镜像
  docker-compose.ci.yml                      # CI 环境编排

修改文件 (15):
  backend/storage/models.py                  # +is_deleted, version, valid_from/valid_to
  backend/storage/database.py                # 新表 init
  backend/milvus/client.py                   # +delete_by_chunk_ids
  backend/storage/graph_client.py            # +cleanup_orphan_edges, merge_entities
  backend/documents/graph_extractor.py       # +valid_from/valid_to 字段
  backend/rag/graph_retriever.py             # +时间过滤 Cypher
  backend/rag/utils.py                       # RRF 权重可配置化
  backend/agent/orchestrator.py              # +时序敏感度路由
  backend/api/routes.py                      # 软删除端点改造
  backend/schemas.py                         # +DocumentStatus, DeleteResponse
  backend/storage/graph_ingestion.py         # +valid_from/valid_to 写入
  pyproject.toml                             # +ragas, pandas, pytest
  docker-compose.yml                         # +Neo4j APOC 插件
  .env.example                               # +新环境变量
  frontend/script.js                         # 软删除确认提示
```

---

## Phase 1: 跨库级联软删除与状态同步

### Task 1.1: MySQL 状态机改造 — models.py 增加软删除字段

**Files:**
- Modify: `backend/storage/models.py:39-53`

- [ ] **Step 1: 为 ParentChunk 增加 is_deleted 和 version 字段**

```python
class ParentChunk(Base):
    __tablename__ = "parent_chunks"

    chunk_id: Mapped[str] = mapped_column(String(512), primary_key=True)
    text: Mapped[str] = mapped_column(Text, nullable=False)
    filename: Mapped[str] = mapped_column(String(255), nullable=False, index=True)
    file_type: Mapped[str] = mapped_column(String(50), default="", nullable=False)
    file_path: Mapped[str] = mapped_column(String(1024), default="", nullable=False)
    page_number: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    parent_chunk_id: Mapped[str] = mapped_column(String(512), default="", nullable=False)
    root_chunk_id: Mapped[str] = mapped_column(String(512), default="", nullable=False)
    chunk_level: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    chunk_idx: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    is_deleted: Mapped[bool] = mapped_column(default=False, nullable=False, index=True)
    version: Mapped[int] = mapped_column(Integer, default=1, nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(timezone.utc), nullable=False)
```

- [ ] **Step 2: 新增 DocumentIndex 表（文档级状态追踪），写在 CommunitySummary 类之后**

```python
class DocumentIndex(Base):
    """文档索引表，追踪文档级版本与状态。"""
    __tablename__ = "document_index"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    filename: Mapped[str] = mapped_column(String(255), nullable=False, index=True, unique=True)
    file_hash: Mapped[str] = mapped_column(String(64), nullable=False, default="")
    is_deleted: Mapped[bool] = mapped_column(default=False, nullable=False, index=True)
    version: Mapped[int] = mapped_column(Integer, default=1, nullable=False)
    chunk_count: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(timezone.utc), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(timezone.utc), nullable=False)
```

- [ ] **Step 3: 在 database.py 中注册新表**

Modify `backend/storage/database.py` 的 `init_db()` 函数,确保 `Base.metadata.create_all()` 自动建表（已存在逻辑，只需确认 import 了所有模型类）。

- [ ] **Step 4: 运行验证**

```bash
uv run python -c "from backend.storage.database import engine, Base; Base.metadata.create_all(engine); print('Tables created')"
```

Expected: 控制台无报错，MySQL 中出现 `document_index` 表，`parent_chunks` 表新增 `is_deleted`, `version` 列。

- [ ] **Step 5: Commit**

```bash
git add backend/storage/models.py backend/storage/database.py
git commit -m "feat: add soft-delete fields to ParentChunk and new DocumentIndex table"
```

---

### Task 1.2: 文档生命周期状态机 — doc_lifecycle.py

**Files:**
- Create: `backend/storage/doc_lifecycle.py`
- Create: `tests/test_doc_lifecycle.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_doc_lifecycle.py
import pytest
from unittest.mock import MagicMock, patch
from backend.storage.doc_lifecycle import mark_document_deleted, get_chunk_ids_by_filename

class TestMarkDocumentDeleted:

    @patch("backend.storage.doc_lifecycle.SessionLocal")
    def test_marks_chunks_as_deleted(self, mock_session_factory):
        mock_session = MagicMock()
        mock_session_factory.return_value.__enter__.return_value = mock_session

        mock_chunks = [MagicMock(chunk_id="ck1"), MagicMock(chunk_id="ck2")]
        mock_session.execute.return_value.scalars.return_value.all.return_value = mock_chunks

        result = mark_document_deleted("test.pdf")

        assert result["filename"] == "test.pdf"
        assert result["affected_chunks"] == 2
        assert result["status"] == "soft_deleted"
        mock_session.commit.assert_called()

    @patch("backend.storage.doc_lifecycle.SessionLocal")
    def test_returns_empty_when_no_chunks(self, mock_session_factory):
        mock_session = MagicMock()
        mock_session_factory.return_value.__enter__.return_value = mock_session
        mock_session.execute.return_value.scalars.return_value.all.return_value = []

        result = mark_document_deleted("nonexist.pdf")

        assert result["affected_chunks"] == 0


class TestGetChunkIdsByFilename:

    @patch("backend.storage.doc_lifecycle.SessionLocal")
    def test_returns_chunk_ids(self, mock_session_factory):
        mock_session = MagicMock()
        mock_session_factory.return_value.__enter__.return_value = mock_session
        mock_session.execute.return_value.scalars.return_value.all.return_value = [
            ("ck1",), ("ck2",), ("ck3",)
        ]

        result = get_chunk_ids_by_filename("test.pdf")

        assert result == ["ck1", "ck2", "ck3"]
```

- [ ] **Step 2: Run test to verify it fails**

```bash
uv run pytest tests/test_doc_lifecycle.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'backend.storage.doc_lifecycle'`

- [ ] **Step 3: 实现 doc_lifecycle.py**

```python
"""文档生命周期管理：软删除、chunk ID 查询。"""
from datetime import datetime, timezone
from sqlalchemy import update, select
from backend.storage.database import SessionLocal
from backend.storage.models import ParentChunk, DocumentIndex


def get_chunk_ids_by_filename(filename: str, include_deleted: bool = False) -> list[str]:
    """获取文档的所有 L3 chunk ID。"""
    with SessionLocal() as session:
        stmt = select(ParentChunk.chunk_id).where(
            ParentChunk.filename == filename,
            ParentChunk.chunk_level == 3,
        )
        if not include_deleted:
            stmt = stmt.where(ParentChunk.is_deleted == False)
        rows = session.execute(stmt).scalars().all()
        return [r[0] if isinstance(r, tuple) else r for r in rows]


def mark_document_deleted(filename: str) -> dict:
    """软删除文档：标记 ParentChunk + DocumentIndex。"""
    with SessionLocal() as session:
        now = datetime.now(timezone.utc)

        # 标记 ParentChunk
        stmt = (
            update(ParentChunk)
            .where(ParentChunk.filename == filename, ParentChunk.is_deleted == False)
            .values(is_deleted=True, version=ParentChunk.version + 1, updated_at=now)
        )
        result = session.execute(stmt)

        # 标记 DocumentIndex
        doc = session.query(DocumentIndex).filter_by(filename=filename).first()
        if doc:
            doc.is_deleted = True
            doc.version += 1
            doc.updated_at = now

        session.commit()

        return {
            "filename": filename,
            "affected_chunks": result.rowcount,
            "status": "soft_deleted",
            "deleted_at": now.isoformat(),
        }
```

- [ ] **Step 4: Run test to verify it passes**

```bash
uv run pytest tests/test_doc_lifecycle.py -v
```

Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add backend/storage/doc_lifecycle.py tests/test_doc_lifecycle.py tests/__init__.py
git commit -m "feat: add document lifecycle manager with soft-delete"
```

---

### Task 1.3: Milvus 批量删除 & Neo4j 边清理

**Files:**
- Modify: `backend/milvus/client.py:83-85` (已有 delete 方法，增加批量包装)
- Create: `backend/storage/graph_cleanup.py`

- [ ] **Step 1: 为 MilvusManager 增加按 chunk_ids 批量删除方法**

在 `backend/milvus/client.py` 的 `delete` 方法后添加:

```python
def delete_by_chunk_ids(self, chunk_ids: list[str]) -> int:
    """按 chunk_id 列表批量删除向量。返回删除条数。"""
    if not chunk_ids:
        return 0
    ids_str = ", ".join(f'"{cid}"' for cid in chunk_ids)
    filter_expr = f"chunk_id in [{ids_str}]"
    res = self.delete(filter_expr)
    return res.get("delete_count", 0) if isinstance(res, dict) else 0
```

- [ ] **Step 2: 编写 Neo4j 图清理模块**

```python
# backend/storage/graph_cleanup.py
"""Neo4j 图清理：移除失效 chunk 引用、回收孤立节点。"""
from backend.storage.graph_client import run_cypher, write_cypher


def strip_chunk_from_edges(chunk_ids: list[str]) -> dict:
    """从 RELATES_TO 边的 source_chunks 中移除指定 chunk ID。"""
    cypher = """
    MATCH ()-[r:RELATES_TO]->()
    WHERE any(cid IN r.source_chunks WHERE cid IN $chunk_ids)
    SET r.source_chunks = [cid IN r.source_chunks WHERE NOT cid IN $chunk_ids]
    RETURN count(r) AS updated_edges
    """
    records = run_cypher(cypher, {"chunk_ids": chunk_ids})
    return {"updated_edges": records[0]["updated_edges"] if records else 0}


def remove_empty_edges() -> int:
    """删除 source_chunks 为空的关系边。"""
    cypher = """
    MATCH ()-[r:RELATES_TO]->()
    WHERE size(r.source_chunks) = 0
    DELETE r
    RETURN count(r) AS deleted
    """
    records = write_cypher(cypher)
    return records.get("relationships_created", 0)  # consume 的汇总


def remove_orphan_entities() -> int:
    """删除没有任何关系的孤立实体节点。"""
    cypher = """
    MATCH (e:Entity)
    WHERE NOT (e)--()
    DELETE e
    RETURN count(e) AS deleted
    """
    records = write_cypher(cypher)
    return records.get("nodes_created", 0)


def full_cascade_cleanup(chunk_ids: list[str]) -> dict:
    """完整级联清理流程。"""
    strip_result = strip_chunk_from_edges(chunk_ids)
    empty_deleted = remove_empty_edges()
    orphan_deleted = remove_orphan_entities()
    return {
        "edges_updated": strip_result["updated_edges"],
        "empty_edges_deleted": empty_deleted,
        "orphan_nodes_deleted": orphan_deleted,
    }
```

- [ ] **Step 3: 验证 Neo4j 清理逻辑正确**

```bash
uv run python -c "
from backend.storage.graph_cleanup import full_cascade_cleanup
result = full_cascade_cleanup([])
print('Empty test:', result)
"
```

Expected: `{'edges_updated': 0, 'empty_edges_deleted': 0, 'orphan_nodes_deleted': 0}`

- [ ] **Step 4: Commit**

```bash
git add backend/milvus/client.py backend/storage/graph_cleanup.py
git commit -m "feat: add Milvus batch delete and Neo4j cascade cleanup"
```

---

### Task 1.4: API 端点改造 — DELETE 改为软删除级联

**Files:**
- Modify: `backend/api/routes.py` — DELETE 端点
- Modify: `backend/schemas.py` — 新增 Pydantic model

- [ ] **Step 1: 新增 schemas**

在 `backend/schemas.py` 末尾添加:

```python
class DeleteDocumentResponse(BaseModel):
    filename: str
    status: str
    affected_chunks: int
    milvus_deleted: int
    graph_edges_updated: int
    graph_empty_edges_deleted: int
    graph_orphan_nodes_deleted: int


class DocumentStatus(BaseModel):
    filename: str
    is_deleted: bool
    version: int
    chunk_count: int
    updated_at: str
```

- [ ] **Step 2: 修改 DELETE 路由**

修改 `backend/api/routes.py` 中的 `DELETE /documents/{filename}` 端点:

```python
@router.delete("/documents/{filename}")
async def delete_document(filename: str) -> DeleteDocumentResponse:
    from backend.storage.doc_lifecycle import mark_document_deleted, get_chunk_ids_by_filename
    from backend.storage.graph_cleanup import full_cascade_cleanup
    from backend.milvus.client import MilvusManager

    # 1. 数据库软删除
    result = mark_document_deleted(filename)
    if result["affected_chunks"] == 0:
        raise HTTPException(status_code=404, detail=f"文档 '{filename}' 不存在或已删除")

    # 2. 获取 chunk IDs 用于 Milvus + Neo4j 清理
    chunk_ids = get_chunk_ids_by_filename(filename)

    # 3. Milvus 向量删除
    milvus = MilvusManager()
    milvus.init_collection()
    milvus_deleted = milvus.delete_by_chunk_ids(chunk_ids)

    # 4. Neo4j 图清理
    graph_result = full_cascade_cleanup(chunk_ids)

    return DeleteDocumentResponse(
        filename=filename,
        status=result["status"],
        affected_chunks=result["affected_chunks"],
        milvus_deleted=milvus_deleted,
        graph_edges_updated=graph_result["edges_updated"],
        graph_empty_edges_deleted=graph_result["empty_edges_deleted"],
        graph_orphan_nodes_deleted=graph_result["orphan_nodes_deleted"],
    )
```

- [ ] **Step 3: 修改检索过滤，排除已删除 chunk**

修改 `backend/rag/utils.py` 的 `retrieve_documents` 函数，在 `filter_expr` 追加排除条件:

```python
# 修改第 334 行
filter_expr = f"chunk_level == {LEAF_RETRIEVE_LEVEL}"
# 改为
filter_expr = f"(chunk_level == {LEAF_RETRIEVE_LEVEL}) && (is_deleted == false)"
```

同时在 `backend/milvus/client.py` 的 `init_collection` 动态 schema 中添加标量字段:

```python
schema.add_field("is_deleted", DataType.BOOL)
```

- [ ] **Step 4: 验证 API 响应**

```bash
# 启动服务后测试
curl -X DELETE http://127.0.0.1:8000/documents/test.pdf
```

Expected: 返回 JSON 包含 `status: "soft_deleted"` 及各清理数量。

- [ ] **Step 5: Commit**

```bash
git add backend/api/routes.py backend/schemas.py backend/rag/utils.py backend/milvus/client.py
git commit -m "feat: cascade soft-delete API with Milvus+Neo4j cleanup"
```

---

## Phase 2: 实体自动消歧与融合

### Task 2.1: 基于编辑距离与向量相似度的快速召回

**Files:**
- Create: `backend/graph/entity_resolution.py`

- [ ] **Step 1: 实现快速召回阶段**

```python
# backend/graph/entity_resolution.py
"""实体消歧：两阶段同义词检测 + Cypher 节点融合。"""
import os
from difflib import SequenceMatcher
from backend.storage.graph_client import run_cypher, write_cypher
from backend.embedding.service import EmbeddingService


SIMILARITY_THRESHOLD = float(os.getenv("ENTITY_SIM_THRESHOLD", "0.75"))


def find_candidates_in_community(community_id: str = None) -> list[dict]:
    """阶段一：从社区内通过编辑距离召回可疑同义词对。"""
    cypher = """
    MATCH (a:Entity)
    WHERE a.community_id IS NOT NULL
    MATCH (b:Entity)
    WHERE b.community_id = a.community_id AND id(a) < id(b)
    WITH a, b, a.community_id AS cid
    WHERE a.name <> b.name
    RETURN a.name AS name_a, b.name AS name_b, cid AS community_id
    LIMIT 500
    """
    records = run_cypher(cypher)
    candidates = []
    for r in records:
        ratio = SequenceMatcher(None, r["name_a"].lower(), r["name_b"].lower()).ratio()
        if ratio >= SIMILARITY_THRESHOLD:
            candidates.append({
                "name_a": r["name_a"],
                "name_b": r["name_b"],
                "community_id": r["community_id"],
                "edit_ratio": round(ratio, 3),
            })
    return candidates


def resolve_entities_batch(candidates: list[dict]) -> list[dict]:
    """阶段二：LLM 确认同义关系。返回确认要合并的实体对。"""
    if not candidates:
        return []

    from backend.agent.orchestrator import _get_worker_model

    model = _get_worker_model()
    confirmed = []

    for c in candidates[:20]:  # 批量限制
        prompt = (
            f"节点A: '{c['name_a']}'，节点B: '{c['name_b']}'。"
            f"编辑距离相似度: {c['edit_ratio']}。"
            f"它们属于同一社区 (community_id={c['community_id']})。"
            f"请判断这两个名称是否指代同一实体。"
            f"只回复 'YES' 或 'NO'。"
        )
        try:
            response = model.invoke(prompt)
            if response and hasattr(response, "content"):
                text = response.content.strip().upper()
                if "YES" in text[:10]:
                    confirmed.append(c)
        except Exception:
            continue

    return confirmed
```

- [ ] **Step 2: 实现 Cypher 节点融合**

在 `entity_resolution.py` 中继续:

```python
def merge_entity_pair(name_a: str, name_b: str) -> dict:
    """将 name_b 合并到 name_a，继承所有边。"""
    cypher = """
    MATCH (a:Entity {name: $name_a})
    MATCH (b:Entity {name: $name_b})
    OPTIONAL MATCH (b)-[r_in:RELATES_TO]->(other)
    OPTIONAL MATCH (other2)-[r_out:RELATES_TO]->(b)
    FOREACH (_ IN CASE WHEN r_in IS NOT NULL THEN [1] ELSE [] END |
      MERGE (a)-[new_r:RELATES_TO]->(other)
      SET new_r = properties(r_in)
    )
    FOREACH (_ IN CASE WHEN r_out IS NOT NULL THEN [1] ELSE [] END |
      MERGE (other2)-[new_r:RELATES_TO]->(a)
      SET new_r = properties(r_out)
    )
    DETACH DELETE b
    RETURN a.name AS merged_into
    """
    records = write_cypher(cypher, {"name_a": name_a, "name_b": name_b})
    return {"merged_into": name_a, "removed": name_b, **records}


def run_entity_resolution() -> dict:
    """完整消歧流程。"""
    candidates = find_candidates_in_community()
    confirmed = resolve_entities_batch(candidates)
    results = []
    for pair in confirmed:
        result = merge_entity_pair(pair["name_a"], pair["name_b"])
        results.append(result)
    return {
        "candidates_found": len(candidates),
        "llm_confirmed": len(confirmed),
        "merged_pairs": results,
    }
```

- [ ] **Step 3: 创建独立运行脚本**

```python
# scripts/run_entity_resolution.py
"""离线实体消歧脚本。"""
import sys
sys.path.insert(0, ".")
from backend.graph.entity_resolution import run_entity_resolution

if __name__ == "__main__":
    result = run_entity_resolution()
    print(f"候选对: {result['candidates_found']}")
    print(f"LLM 确认: {result['llm_confirmed']}")
    for r in result["merged_pairs"]:
        print(f"  合并: {r['merged_into']} ← {r['removed']}")
```

- [ ] **Step 4: Commit**

```bash
git add backend/graph/entity_resolution.py scripts/run_entity_resolution.py
git commit -m "feat: two-stage entity resolution with edit-distance + LLM + Cypher merge"
```

---

### Task 2.2: 实体消歧单元测试

**Files:**
- Create: `tests/test_entity_resolution.py`

- [ ] **Step 1: 写测试**

```python
# tests/test_entity_resolution.py
import pytest
from backend.graph.entity_resolution import find_candidates_in_community


class TestEntityResolution:

    def test_find_candidates_returns_list(self):
        """快速召回返回列表格式。"""
        candidates = find_candidates_in_community()
        assert isinstance(candidates, list)

    def test_candidates_have_required_fields(self):
        """候选对包含必要字段。"""
        candidates = find_candidates_in_community()
        for c in candidates[:5]:
            assert "name_a" in c
            assert "name_b" in c
            assert "edit_ratio" in c
            assert c["edit_ratio"] >= 0.75
```

- [ ] **Step 2: Run test**

```bash
uv run pytest tests/test_entity_resolution.py -v
```

Expected: 2 passed (Neo4j 可用时) 或 skipped

- [ ] **Step 3: Commit**

```bash
git add tests/test_entity_resolution.py
git commit -m "test: entity resolution candidate detection"
```

---

## Phase 3: 时序知识图谱增强

### Task 3.1: 抽取 Schema 扩展 — 增加 valid_from/valid_to

**Files:**
- Modify: `backend/documents/graph_extractor.py:7-23` (EntityInfo, RelationInfo)
- Modify: `backend/storage/graph_ingestion.py` (写入 valid_from/valid_to)

- [ ] **Step 1: 扩展 Pydantic Schema**

修改 `graph_extractor.py` 中的 `EntityInfo` 和 `RelationInfo`:

```python
class EntityInfo(BaseModel):
    name: str = Field(description="实体名")
    type: str = Field(description="实体类型: Person|Organization|Technology|Concept|Model|Method|Data")
    description: str = Field(default="", description="简短描述")
    valid_from: str = Field(default="", description="知识生效起始年份，如 '2023'")
    valid_to: str = Field(default="", description="知识生效截止年份，如 '2025'，仍有效则留空")


class RelationInfo(BaseModel):
    subject: str = Field(description="源实体名")
    predicate: str = Field(description="关系: DEPENDS_ON|CONTAINS|CITES|USES|PART_OF|PROPOSES|EVALUATES")
    object: str = Field(description="目标实体名")
    description: str = Field(default="", description="关系描述")
    weight: float = Field(default=0.5, description="置信度 0-1")
    valid_from: str = Field(default="", description="关系生效起始年份")
    valid_to: str = Field(default="", description="关系生效截止年份")
```

- [ ] **Step 2: 修改抽取 Prompt**

修改 `EXTRACTION_PROMPT`，在规则 3 之后插入:

```python
EXTRACTION_PROMPT = """你是一个知识图谱构建专家。请从以下文本中提取实体和关系。

规则:
1. 实体: 提取文本中出现的所有重要名词——包含人名、机构、技术、概念、产品、模型、算法、数据、方法。
2. 关系: 识别实体之间的语义关系。
3. 消歧: 如果多个名称指向同一实体，使用最规范的名称。
4. 时间: 如果文本中明确了该知识的时间范围（如"2023年发布"、"2020-2024年间使用"），填写 valid_from 和 valid_to。不明确则留空。
5. 只提取文本中明确出现的信息，不要编造。
6. 限制: 最多提取 10 个实体和 15 条关系。"""
```

- [ ] **Step 3: 修改 graph_ingestion.py 写入时间字段**

在 `backend/storage/graph_ingestion.py` 的 MERGE 实体语句中追加属性:

修改 `ingest_extraction_result` 函数中的 Cypher MERGE:

```python
# 实体 MERGE 追加
SET e.description = $entity.description,
    e.type = $entity.type,
    e.valid_from = CASE WHEN $entity.valid_from <> '' THEN $entity.valid_from ELSE e.valid_from END,
    e.valid_to = CASE WHEN $entity.valid_to <> '' THEN $entity.valid_to ELSE e.valid_to END

# 关系 MERGE 追加
SET r.description = $rel.description,
    r.weight = CASE WHEN $rel.weight > r.weight THEN $rel.weight ELSE r.weight END,
    r.valid_from = CASE WHEN $rel.valid_from <> '' THEN $rel.valid_from ELSE r.valid_from END,
    r.valid_to = CASE WHEN $rel.valid_to <> '' THEN $rel.valid_to ELSE r.valid_to END,
    r.source_chunks = CASE
        WHEN $chunk_id IN r.source_chunks THEN r.source_chunks
        ELSE r.source_chunks + $chunk_id
    END
```

- [ ] **Step 4: Commit**

```bash
git add backend/documents/graph_extractor.py backend/storage/graph_ingestion.py
git commit -m "feat: add valid_from/valid_to temporal fields to graph extraction"
```

---

### Task 3.2: 时序图检索 — graph_retriever.py 增加时间过滤

**Files:**
- Modify: `backend/rag/graph_retriever.py:6-78` (local_graph_search)

- [ ] **Step 1: 为 local_graph_search 增加时间过滤参数**

```python
def local_graph_search(
    query: str,
    top_k: int = 5,
    graph_hops: int = 1,
    time_filter: dict = None,  # {"year": "2024", "mode": "at_or_after"}
) -> dict:
```

- [ ] **Step 2: 在 Step 2 (Graph expansion) 的 Cypher 中追加时间过滤**

在 Step 2 的第一个 cypher 查询中，在 `RETURN` 前插入时间条件:

```python
time_clause = ""
time_params = {"chunk_ids": chunk_ids}
if time_filter and time_filter.get("year"):
    year = str(time_filter["year"])
    if time_filter.get("mode") == "at_or_after":
        time_clause = """
        AND (r.valid_from = '' OR r.valid_from <= $filter_year)
        AND (r.valid_to = '' OR r.valid_to >= $filter_year)
        """
    elif time_filter.get("mode") == "before":
        time_clause = """
        AND r.valid_to <> '' AND r.valid_to < $filter_year
        """
    elif time_filter.get("mode") == "exact":
        time_clause = """
        AND (r.valid_from = $filter_year OR r.valid_to = $filter_year)
        """
    time_params["filter_year"] = year

cypher = f"""
MATCH (a:Entity)-[r:RELATES_TO]->(b:Entity)
WHERE any(cid IN r.source_chunks WHERE cid IN $chunk_ids)
{time_clause}
RETURN a.name AS subject, r.predicate AS predicate, b.name AS object,
       r.description AS desc, r.weight AS weight
LIMIT 30
"""
```

- [ ] **Step 3: Commit**

```bash
git add backend/rag/graph_retriever.py
git commit -m "feat: add temporal filtering to local graph search"
```

---

### Task 3.3: Supervisor 时序敏感度路由

**Files:**
- Modify: `backend/agent/orchestrator.py:226-277` (supervisor_node)

- [ ] **Step 1: 修改 Supervisor System Prompt**

在 orchestrator.py 的 SUPERVISOR_SYSTEM_PROMPT 中追加时序路由规则:

在 Prompt 的 "路由规则" 段落后追加:

```
- 时序路由: 如果用户问题涉及时间跨度（如"去年"、"2023年到2025年"、"最近"、"最新的"），优先路由到 `local_graph_search`，并提示该 Agent 启用时间过滤。
```

- [ ] **Step 2: 修改 SupervisorRoute Schema 增加时序标记**

```python
class SupervisorRoute(BaseModel):
    routes: list[str] = Field(description="路由目标列表: rag_specialist|web_searcher|direct_answer|data_analyst|local_graph_search|global_graph_search")
    reason: str = Field(description="路由理由")
    is_temporal: bool = Field(default=False, description="是否为时序敏感查询")
    temporal_year: str = Field(default="", description="时序查询目标年份")
```

- [ ] **Step 3: 在 local_graph_search_node 传递 time_filter**

修改 `orchestrator.py:498` 的 `local_graph_search_node`:

```python
def local_graph_search_node(state: SupervisorState) -> dict:
    user_query = state.get("user_query", "")
    agent_trace = state.get("agent_trace") or {}

    time_filter = None
    if agent_trace.get("routing_is_temporal") and agent_trace.get("routing_temporal_year"):
        time_filter = {
            "year": agent_trace["routing_temporal_year"],
            "mode": "at_or_after",
        }

    from backend.rag.graph_retriever import local_graph_search
    result = local_graph_search(user_query, time_filter=time_filter)
    ...
```

- [ ] **Step 4: Commit**

```bash
git add backend/agent/orchestrator.py
git commit -m "feat: add temporal sensitivity routing to supervisor"
```

---

## Phase 4: 自动化 RAG 评估管道

### Task 4.1: 评估模块 & 黄金数据集

**Files:**
- Create: `backend/evaluation/__init__.py`
- Create: `backend/evaluation/dataset.py`
- Create: `tests/golden_dataset.json`

- [ ] **Step 1: 黄金数据集 JSON Schema**

```json
[
  {
    "id": "Q001",
    "question": "什么是 GraphRAG？",
    "ground_truth": "GraphRAG 是结合知识图谱和向量检索的 RAG 增强方案，通过实体关系网络实现多跳推理。",
    "source_doc": "docs/graphrag_overview.pdf",
    "query_type": "conceptual"
  },
  {
    "id": "Q002",
    "question": "Ragent AI 用了什么向量数据库？",
    "ground_truth": "Ragent AI 使用 Milvus 2.5 作为向量数据库，支持 HNSW + SPARSE_INVERTED_INDEX 混合检索。",
    "source_doc": "docs/README.md",
    "query_type": "detail"
  }
]
```

- [ ] **Step 2: 编写数据集加载器**

```python
# backend/evaluation/dataset.py
"""黄金数据集加载器。"""
import json
from pathlib import Path

DATASET_PATH = Path(__file__).parent.parent.parent / "tests" / "golden_dataset.json"


def load_golden_dataset() -> list[dict]:
    with open(DATASET_PATH, "r", encoding="utf-8") as f:
        return json.load(f)


def get_questions() -> list[str]:
    return [item["question"] for item in load_golden_dataset()]


def get_ground_truths() -> list[str]:
    return [item["ground_truth"] for item in load_golden_dataset()]
```

- [ ] **Step 3: 编写评估指标计算模块**

```python
# backend/evaluation/metrics.py
"""RAG 评估指标计算（基于 Ragas 框架）。"""
import os
from datasets import Dataset
from ragas import evaluate
from ragas.metrics import faithfulness, answer_relevancy, context_precision
from langchain.chat_models import init_chat_model

LLM = init_chat_model(
    model=os.getenv("GRADE_MODEL", os.getenv("MODEL", "qwen-plus")),
    model_provider="openai",
    api_key=os.getenv("ARK_API_KEY"),
    base_url=os.getenv("BASE_URL"),
    temperature=0.0,
)


def compute_ragas_metrics(data_samples: list[dict]) -> dict:
    """计算 context_precision, faithfulness, answer_relevancy。"""
    dataset = Dataset.from_list(data_samples)
    result = evaluate(
        dataset=dataset,
        metrics=[context_precision, faithfulness, answer_relevancy],
        llm=LLM,
    )
    return {k: round(float(v), 4) for k, v in result.items()}
```

- [ ] **Step 4: Commit**

```bash
git add backend/evaluation/ tests/golden_dataset.json
git commit -m "feat: add RAG evaluation module with golden dataset and Ragas metrics"
```

---

### Task 4.2: 评估入口脚本

**Files:**
- Create: `scripts/run_evaluation.py`
- Modify: `pyproject.toml` (添加 ragas 依赖)

- [ ] **Step 1: 添加依赖**

在 `pyproject.toml` 的 `dependencies` 中添加:

```toml
"ragas>=0.2.0",
"datasets>=2.19.0",
"pandas>=2.2.0",
```

安装:

```bash
uv sync
```

- [ ] **Step 2: 编写评估脚本**

```python
#!/usr/bin/env python3
# scripts/run_evaluation.py
"""自动化 RAG 评估入口脚本。"""
import json, sys, time
sys.path.insert(0, ".")

from backend.rag.utils import retrieve_documents
from backend.evaluation.dataset import load_golden_dataset
from backend.evaluation.metrics import compute_ragas_metrics


def run_evaluation() -> dict:
    dataset = load_golden_dataset()
    print(f"基准数据集: {len(dataset)} 条")

    samples = []
    for item in dataset:
        print(f"  评估 {item['id']}: {item['question'][:50]}...")
        t0 = time.time()
        result = retrieve_documents(item["question"])
        docs = result.get("docs", [])
        context_texts = [d.get("text", "") for d in docs]

        # 模拟 answer — 实际使用中替换为真实 Agent 回答
        samples.append({
            "question": item["question"],
            "answer": item["ground_truth"],
            "contexts": context_texts,
            "ground_truth": item["ground_truth"],
        })
        print(f"    耗时 {time.time() - t0:.1f}s, 检索 {len(docs)} 条")

    metrics = compute_ragas_metrics(samples)
    return {"metrics": metrics, "sample_count": len(samples)}


if __name__ == "__main__":
    result = run_evaluation()
    print(f"\n===== 评估结果 =====")
    for k, v in result["metrics"].items():
        print(f"  {k}: {v}")
    print(f"\n样本数: {result['sample_count']}")

    # 输出 JSON 便于后续分析
    with open("evaluation_result.json", "w") as f:
        json.dump(result, f, indent=2, ensure_ascii=False)
    print("结果已保存到 evaluation_result.json")
```

- [ ] **Step 3: 验证脚本可运行**

```bash
uv run python scripts/run_evaluation.py
```

Expected: 输出 Ragas 三项指标分数，保存 `evaluation_result.json`。

- [ ] **Step 4: Commit**

```bash
git add scripts/run_evaluation.py pyproject.toml uv.lock
git commit -m "feat: add evaluation runner script with Ragas integration"
```

---

## Phase 5: 三路 RRF 权重网格搜索

### Task 5.1: RRF 权重可配置化

**Files:**
- Modify: `backend/rag/utils.py:73-109` (rrf_fusion_three_channel)
- Modify: `.env.example` (新增配置项)

- [ ] **Step 1: 使 RRF 权重从环境变量读取**

修改 `utils.py` 的 `rrf_fusion_three_channel`:

```python
RRF_WEIGHT_DENSE = float(os.getenv("RRF_WEIGHT_DENSE", "0.4"))
RRF_WEIGHT_SPARSE = float(os.getenv("RRF_WEIGHT_SPARSE", "0.3"))
RRF_WEIGHT_GRAPH = float(os.getenv("RRF_WEIGHT_GRAPH", "0.3"))


def rrf_fusion_three_channel(
    dense_results: list,
    sparse_results: list,
    graph_results: list,
    k: int = 60,
    weights: tuple = None,
    top_k: int = 10,
) -> list:
    if weights is None:
        weights = (RRF_WEIGHT_DENSE, RRF_WEIGHT_SPARSE, RRF_WEIGHT_GRAPH)
    w1, w2, w3 = weights
    # ... 其余不变
```

- [ ] **Step 2: 更新 .env.example**

追加:

```env
# ===== RRF 三通道权重 =====
RRF_WEIGHT_DENSE=0.4
RRF_WEIGHT_SPARSE=0.3
RRF_WEIGHT_GRAPH=0.3
```

- [ ] **Step 3: Commit**

```bash
git add backend/rag/utils.py .env.example
git commit -m "feat: make RRF weights configurable via env vars"
```

---

### Task 5.2: 网格搜索脚本

**Files:**
- Create: `scripts/grid_search_rrf.py`

- [ ] **Step 1: 编写网格搜索脚本**

```python
#!/usr/bin/env python3
# scripts/grid_search_rrf.py
"""RRF 三通道权重网格搜索。以 0.1 为步长遍历权重组合。"""
import json, sys, itertools
sys.path.insert(0, ".")

from backend.evaluation.dataset import load_golden_dataset
from backend.evaluation.metrics import compute_ragas_metrics
from backend.rag.utils import retrieve_documents, rrf_fusion_three_channel
from backend.embedding.service import EmbeddingService
from backend.rag.graph_retriever import local_graph_search

embedding_service = EmbeddingService()


def evaluate_with_weights(w1, w2, w3, dataset) -> float:
    """用指定权重跑完整评估，返回平均 context_precision。"""
    samples = []
    for item in dataset:
        # 采集三路结果
        dense_vec = embedding_service.get_embeddings([item["question"]])[0]
        sparse_vec = embedding_service.get_sparse_embedding(item["question"])

        # 用临时权重做融合
        from backend.milvus.client import MilvusManager
        milvus = MilvusManager()
        dense_result = milvus.dense_retrieve(dense_vec, top_k=10)
        sparse_result = milvus.hybrid_retrieve(dense_vec, sparse_vec, top_k=10)

        fused = rrf_fusion_three_channel(
            dense_result, sparse_result, [],
            weights=(w1, w2, w3), top_k=5,
        )
        context_texts = [d.get("text", "") for d in fused]

        samples.append({
            "question": item["question"],
            "answer": item["ground_truth"],
            "contexts": context_texts,
            "ground_truth": item["ground_truth"],
        })

    metrics = compute_ragas_metrics(samples)
    return metrics.get("context_precision", 0.0)


def main():
    dataset = load_golden_dataset()
    print(f"数据集: {len(dataset)} 条")
    print(f"权重步长: 0.1, 组合数: {sum(1 for _ in itertools.product([round(x*0.1,1) for x in range(0,11)], repeat=3) if abs(sum(_)-1.0)<0.001)}")

    best_score = 0.0
    best_weights = (0.4, 0.3, 0.3)
    results = []

    # 归一化约束: w1 + w2 + w3 = 1.0
    values = [round(x * 0.1, 1) for x in range(0, 11)]
    for w1, w2, w3 in itertools.product(values, repeat=3):
        if abs(w1 + w2 + w3 - 1.0) > 0.001:
            continue  # 跳过不满足归一化约束的组合
        print(f"测试权重: ({w1}, {w2}, {w3})...", end=" ")
        try:
            score = evaluate_with_weights(w1, w2, w3, dataset)
            results.append({"weights": (w1, w2, w3), "context_precision": score})
            print(f"score={score:.4f}")
            if score > best_score:
                best_score = score
                best_weights = (w1, w2, w3)
        except Exception as e:
            print(f"error: {e}")

    print(f"\n===== 最佳权重 =====")
    print(f"DENSE={best_weights[0]}, SPARSE={best_weights[1]}, GRAPH={best_weights[2]}")
    print(f"Context Precision: {best_score:.4f}")

    with open("grid_search_result.json", "w") as f:
        json.dump({
            "best_weights": {"dense": best_weights[0], "sparse": best_weights[1], "graph": best_weights[2]},
            "best_score": best_score,
            "all_results": results,
        }, f, indent=2, ensure_ascii=False)
    print("结果已保存到 grid_search_result.json")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验证脚本语法正确**

```bash
uv run python -c "import scripts.grid_search_rrf"
```

Expected: 无 ImportError 或 SyntaxError。

- [ ] **Step 3: Commit**

```bash
git add scripts/grid_search_rrf.py
git commit -m "feat: add RRF weight grid search script"
```

---

## Phase 6: GitOps 与 CI/CD 集成

### Task 6.1: Dockerfile 应用镜像

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: 编写 Dockerfile**

```dockerfile
FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml uv.lock ./
RUN pip install --no-cache-dir uv && uv sync --frozen

COPY . .

EXPOSE 8000

CMD ["uv", "run", "python", "start.py"]
```

- [ ] **Step 2: 编写 CI 环境 docker-compose**

```yaml
# docker-compose.ci.yml
version: '3.5'
services:
  mysql-ci:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: testpass
      MYSQL_DATABASE: agent_chat_test
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis-ci:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  milvus-ci:
    image: milvusdb/milvus:v2.5.0-beta
    ports:
      - "19530:19530"
    environment:
      ETCD_ENDPOINTS: etcd-ci:2379
      MINIO_ADDRESS: minio-ci:9000

  etcd-ci:
    image: quay.io/coreos/etcd:v3.5.18
    environment:
      ETCD_AUTO_COMPACTION_MODE: revision
      ETCD_AUTO_COMPACTION_RETENTION: 1000

  minio-ci:
    image: minio/minio:RELEASE.2024-05-28T17-19-04Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    command: server /data

  neo4j-ci:
    image: neo4j:5.26-community
    environment:
      NEO4J_AUTH: neo4j/testpass
    ports:
      - "7474:7474"
      - "7687:7687"
```

- [ ] **Step 3: Commit**

```bash
git add Dockerfile docker-compose.ci.yml
git commit -m "feat: add Dockerfile and CI docker-compose"
```

---

### Task 6.2: GitHub Actions CI 流水线

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: 编写 CI 工作流**

```yaml
name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      PYTHONPATH: .
      DATABASE_URL: mysql+pymysql://root:testpass@localhost:3306/agent_chat_test
      REDIS_URL: redis://localhost:6379/0
      MILVUS_URI: http://localhost:19530
      MILVUS_COLLECTION: test_collection
      NEO4J_URI: bolt://localhost:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: testpass

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Start services
        run: |
          docker compose -f docker-compose.ci.yml up -d --wait
          sleep 10

      - name: Init database
        run: |
          uv run python -c "
          from backend.storage.database import engine, Base
          from backend.storage.models import *
          Base.metadata.create_all(engine)
          print('Database initialized')
          "

      - name: Run unit tests
        run: uv run pytest tests/ -v --tb=short

      - name: Run import verification
        run: |
          uv run python -c "
          from backend.agent.orchestrator import supervisor_node
          from backend.rag.utils import retrieve_documents
          from backend.storage.graph_client import run_cypher
          from backend.milvus.client import MilvusManager
          from backend.evaluation.metrics import compute_ragas_metrics
          print('All imports successful')
          "

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v
```

- [ ] **Step 2: 验证 CI 配置语法**

```bash
# 本地验证（需要 GitHub CLI）
gh workflow lint .github/workflows/ci.yml
```

Expected: "Workflow is valid"

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "feat: add GitHub Actions CI pipeline"
```

---

### Task 6.3: pyproject.toml 测试配置

**Files:**
- Modify: `pyproject.toml` (追加 pytest 配置)

- [ ] **Step 1: 追加 pytest 配置**

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short --strict-markers"
```

- [ ] **Step 2: 验证测试配置**

```bash
uv run pytest --collect-only
```

Expected: 列出所有已收集的测试用例。

- [ ] **Step 3: Commit**

```bash
git add pyproject.toml
git commit -m "chore: add pytest configuration to pyproject.toml"
```

---

## 验收标准

### Phase 1 — 跨库级联软删除
- [ ] 删除文档后，MySQL `parent_chunks` 表中对应行 `is_deleted=1`
- [ ] Milvus 中对应向量被删除（query 返回空）
- [ ] Neo4j 中引用已删除 chunk 的边被清理
- [ ] 检索时不会返回已删除 chunk 的内容

### Phase 2 — 实体消歧
- [ ] `run_entity_resolution.py` 成功合并至少 1 对同义实体
- [ ] 合并后 Neo4j 节点数量减少，边被正确继承
- [ ] 消歧后知识图谱查询不会返回重复实体

### Phase 3 — 时序增强
- [ ] 上传文档后，Neo4j 中 `valid_from`/`valid_to` 属性有值
- [ ] 带时间过滤的查询返回符合时间范围的结果
- [ ] Supervisor 对"2023年"类查询正确路由到 local_graph_search

### Phase 4 — 评估管道
- [ ] `run_evaluation.py` 成功运行并输出三项 Ragas 指标
- [ ] 评估结果保存为 `evaluation_result.json`
- [ ] CI 中评估步骤不报错

### Phase 5 — 网格搜索
- [ ] `grid_search_rrf.py` 遍历所有权重组合
- [ ] 输出最佳权重配置和对应分数
- [ ] 结果保存为 `grid_search_result.json`

### Phase 6 — CI/CD
- [ ] `git push` 触发 GitHub Actions 流水线
- [ ] 流水线中 Docker 服务启动成功
- [ ] 所有单元测试通过
- [ ] 导入验证步骤通过

---

## 执行顺序建议

```
Week 1: Phase 1 (软删除) + Phase 6 (CI/CD) — 并行，底层基础设施
Week 2: Phase 2 (实体消歧) + Phase 3 (时序增强) — 并行，均依赖 Phase 1 的 Neo4j 清理能力
Week 3: Phase 4 (评估管道) — 独立，但需要 Phase 1-3 稳定后才能评估
Week 4: Phase 5 (网格搜索) — 依赖 Phase 4 评估框架
```

Phase 1 和 Phase 6 可以同时开始，两者不冲突。Phase 4 和 Phase 5 建议串行（5 依赖 4 的评估函数）。
